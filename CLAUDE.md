# dev-sergio — Claude Instructions

## Branch Rule

**Always work from the `sergio` branch.**

At the start of every session in this directory, verify the current branch is `sergio`. If it is not, switch to it before doing anything else:

```bash
git checkout sergio
```

All commits, edits, and pushes must target the `sergio` branch — never `dev`, `main`, or any other branch unless explicitly instructed.
